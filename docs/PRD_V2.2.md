# PRD v2.2 — 后期需求规划

> 版本：v2.2 草案 | 日期：2026-07-20 | 状态：规划中

---

## 一、希区柯克镜头 & 特效镜头

### 1.1 目标

智能混剪增加电影级运镜效果，提升视频质感和专业度。

### 1.2 特效镜头类型

| 类型 | 效果 | 适用场景 |
|------|------|---------|
| 希区柯克变焦 (Dolly Zoom) | 主体不变，背景拉近/拉远 | 悬念、强调、转场 |
| 推拉镜头 (Push/Pull) | 画面徐徐推进或拉远 | 开场、产品展示 |
| 摇晃镜头 (Shake) | 画面震动 | 冲击感、动作场景 |
| 慢动作 (Slow Motion) | 逐帧插值降速 | 细节展示、情绪渲染 |
| 快速缩放 (Speed Ramp) | 变速 + 缩放组合 | 卡点、节奏变化 |
| 旋转过渡 (Spin) | 画面旋转切入 | 快节奏转场 |
| 镜像/翻转 (Flip) | 水平/垂直翻转过渡 | 对比、创意转场 |

### 1.3 技术方案

```
ffmpeg 滤镜链：
  zoompan  →  推拉/希区柯克
  minterpolate  →  慢动作
  rotate + zoompan  →  旋转缩放
  setpts/atempo  →  变速
```

### 1.4 智能匹配

根据画面内容自动推荐特效：
- 产品特写 → 慢动作 + 推近
- 人物出场 → 希区柯克
- 快节奏 BGM → 快速缩放
- 文字/字幕出现 → 弹入动画

---

## 二、剪映 (PR) 驱动 & 审美判断

### 2.1 目标

将 AI 生成的粗剪 XML/JSON 导出，驱动剪映进行精细化剪辑。剪辑时做审美判断。

### 2.2 导出格式

- 剪映支持通过 DRT (Draft) 文件导入工程
- 已有 `utils/jianying_exporter.py` 基础，需扩展
- 导出内容：时间轴、转场、滤镜、字幕、音频

### 2.3 审美判断模块

| 维度 | 判断标准 | 技术 |
|------|---------|------|
| 构图 | 三分法、主体居中比例 | `_score_clip()` 已有的主体突出分 |
| 色彩 | 色调统一性、饱和度 | cv2 色彩直方图分析 |
| 节奏 | BPM 与镜头切换卡点 | ffmpeg 提取 BPM，镜头时长对齐 |
| 连贯性 | 相邻镜头的色彩/亮度跳变 | 帧间差异检测 |
| 安全区 | 字幕/标题是否在安全区内 | OCR + 位置分析 |

### 2.4 剪映驱动流程

```
混剪粗剪 → 导出 DRT JSON
  → 剪映打开工程
  → AI 逐镜头审阅：
      构图建议、色彩调整建议、转场建议
  → 用户确认/修改
  → 导出成品
```

### 2.5 接口（以服务端 /guide 文档为准）

| 能力 | 描述 |
|-----|------|
| 导出剪映工程 | 将粗剪时间轴导出为剪映 DRT 文件 |
| 审美评价 | LLM 审美评价（构图/色彩/节奏） |
| 节奏分析 | BPM 卡点对齐分析 |

---

## 三、一键成片模板 + 素材匹配

### 3.1 目标

提供预置视频模板，AI 根据模板结构从素材库中自动匹配素材。
> **状态（2026-08-01）**：🟡 部分实现。`compile_video_page.py` 已实现产品成片、脚本成片与远程素材匹配；`.tpl.json` 模板结构、PR/剪映导入尚未落地。

### 3.2 模板结构

```json
{
  "name": "电商带货-30s",
  "duration": 30,
  "template": [
    {"type": "intro", "duration": 3, "tag": ["logo", "品牌"]},
    {"type": "hook", "duration": 5, "tag": ["产品特写", "痛点展示"]},
    {"type": "feature", "duration": 3, "tag": ["功能介绍", "卖点"]},
    {"type": "feature", "duration": 3, "tag": ["功能介绍", "卖点"]},
    {"type": "feature", "duration": 3, "tag": ["功能介绍", "卖点"]},
    {"type": "testimonial", "duration": 5, "tag": ["使用场景", "真人出镜"]},
    {"type": "cta", "duration": 5, "tag": ["价格", "促销"]},
    {"type": "outro", "duration": 3, "tag": ["logo", "结尾"]}
  ],
  "bgm": "upbeat_electronic",
  "transitions": "fade"
}
```

### 3.3 素材匹配

```
选择模板 → 解析所需 tag → CLIP 向量检索素材库
  → 每个 slot 匹配 top 5 候选
  → 评分 + 去重 → 自动填充
  → 用户可手动替换
```

### 3.4 预设模板

| 模板 | 时长 | 适用 |
|------|------|------|
| 电商带货-15s | 15s | 短视频信息流 |
| 电商带货-30s | 30s | 短视频信息流 |
| 电商带货-60s | 60s | 直播切片混剪 |
| 产品评测-60s | 60s | B 站/小红书 |
| 品牌故事-90s | 90s | 品牌宣传 |
| 活动促销-15s | 15s | 大促快节奏 |

### 3.5 接口（以服务端 /guide 文档为准）

| 能力 | 描述 |
|-----|------|
| 获取模板列表 | 查询可用模板 |
| 素材匹配 | 根据模板 tag 匹配素材库 |
| 编译成片 | 一键编译成片 |

### 3.6 导入 PR / 剪映模板

**目标**：支持导入 Premiere Pro (PR) 和剪映的工程文件作为模板，AI 自动解析结构并适配到素材库。

#### 支持格式

| 来源 | 文件格式 | 解析内容 |
|------|---------|---------|
| 剪映 (CapCut) | `.drt` (Draft JSON) | 时间轴、片段长度、转场、滤镜、文本、音频轨道 |
| Premiere Pro | `.xml` (FCP XML) | 序列结构、剪辑片段、转场、标记 |
| After Effects | `.aep` → `.xml` | 合成结构（间接） |
| 自定义 JSON | `.tpl.json` | 本系统原生格式 |

#### 导入流程

```
用户上传 PR .xml / 剪映 .drt
  → 服务端解析工程结构：
      提取 片段时长、转场类型、BGM、文本模板、层级
  → 转换为内部模板 JSON（3.2 格式）
      每段标注 tag（自动识别或手动标注）
  → 模板存入模板库（可分享、售卖）
  → 使用时自动匹配素材库素材
```

#### 技术方案

**剪映 DRT 解析**（已有 `utils/jianying_exporter.py` 基础，扩展读取方向）：
- DRT 文件本质是 JSON，包含 `tracks`、`segments`、`transitions`
- 提取轨道结构 → 映射到内部模板 slot
- 提取转场类型 → 映射到 ffmpeg xfade 参数
- 提取文本模板 → 作为 overlay 模板

**Premiere Pro XML 解析**：
- FCP XML 标准格式，包含 `<sequence>`、`<clip>`、`<transition>`
- 解析 clip 时长、顺序
- 提取 transition 类型 → 映射

#### 模板分享

| 功能 | 说明 |
|------|------|
| 导出模板 | 内部模板 JSON 可导出为 `.tpl.json` |
| 导入模板 | 支持 `.tpl.json` / `.drt` / `.xml` |
| 模板市场 | 服务端模板库，用户可上传/下载 |
| 模板版本 | 模板可升级，兼容旧版素材 |

### 3.7 接口（以服务端 /guide 文档为准）

| 能力 | 描述 |
|-----|------|
| 导入模板 | 上传 PR/剪映 工程文件，解析为模板 |
| 导出模板 | 导出模板为 .tpl.json |
| 上传模板市场 | 上传模板到模板市场 |
| 模板市场列表 | 查询可用模板 |

---

### 3.8 产品成片页模板交互

`compile_video_page.py` 的“产品成片” tab 按模板流程重新组织：

```
左侧产品区
  → 选择产品（必须）
  → 原“性能参数 / 核心卖点”区域改为“模板库”
  → 点击“卖点文案”按钮可弹窗/浮层展示原核心参数与卖点（供参考）
  → 选择模板后，右侧“模板参数”区域按模板 params 动态生成表单
  → 点击“设置默认”把参数全部恢复为模板默认值
  → 点击“开始执行”提交一键成片任务
```

右侧参数区标题由“可选设置”改为“模板参数”，包含：
- 通用字段：素材目录、配音音频、TTS 音色、片头（MG 开场）、封面、字幕文案、比例、每段时长、成片数量、总时长等。
- 模板自定义字段：根据所选模板的 `params` 定义动态生成（字符串/整数/颜色/JSON 等）。
- “设置默认”按钮：将当前模板的所有参数重置为 `defaults` 或 `params` 中的默认值。

模板选择规则：
- 通过服务端 `/mg/templates` 拉取模板列表（包含内置模板 + 自定义模板）。
- 当从“素材检索”跳转过来且已携带相关素材时，保留现有素材，不强制覆盖；仅按模板填充文案/参数缺省项。
- 未选择模板时沿用原逻辑（不依赖模板）。

提交一键成片任务：
- 在 `compile_video_page._collect_params()` 中增加 `template_id` 和 `template_params` 字段。
- `template_id` 为所选模板 id（内置模板直接使用 `id`，自定义模板使用 `backend`）。
- `template_params` 为当前界面收集到的模板自定义参数值。
- 服务端 `video_montage` 任务按需读取模板参数，用于生成开场/转场/片尾/字幕样式等。


## 四、MG 动画整合

### 4.1 目标

将现有 MG 动画能力（Remotion）整合为一键成片流程。

### 4.2 当前状态

- 服务端已提供 `/mg/*` 接口：`/mg/templates`（模板列表）、`/mg/generate`（生成）、`/mg/status/{id}`（状态）、`/mg/result/{id}`（下载）。
- `studio/utils/mg_server_client.py` 已按 OpenAPI 封装：模板 CRUD、预览、任务提交、状态轮询、结果下载。
- `studio/gui/mg_render_worker.py` 已实现 `/mg/generate` -> `/mg/status` -> `/mg/result` 的轮询下载链路。
- `studio/gui/mg_animation_page.py` 已按服务端模板流程重新设计：左侧模板库（内置 + 自定义）、动态参数表单、模板新建/保存/删除、PNG 预览、MP4 生成。
- 本地 Remotion 工程 `studio/remotion/` 仅作为离线 fallback 保留，不再要求客户端安装 Node/Chrome。

### 4.3 整合方案

```
客户端选择模板 / 新建自定义模板
  → 填写模板参数（通用样式 + 模板特有参数）
    点击“预览”调用 /mg/generate -> /mg/status -> /mg/result 先生成短视频，再用 ffmpeg 提取首帧 PNG 作为预览
  → 点击“生成”调用 /mg/generate -> /mg/status -> /mg/result 下载 MP4
  → 客户端任务列表展示进度/结果，下载完成后可写入素材库或合并到主视频
```

自定义模板：
- 在“模板信息”中选择后端（mg_scene / mg_intro / mg_outro / mg_countdown / mg_quote / mg_benchmark）。
- 通过 JSON 数组定义参数 `{name, type, label, default}`，保存到服务端后出现在“自定义模板”分组。
- 渲染时把 `backend` 作为 `template` 字段提交，并带上自定义参数值。

内置模板参数：
- `mg_scene`：scenes[] 多段文字动画。
- `mg_intro`：title + subtitle 片头。
- `mg_outro`：text + subtext 片尾。
- `mg_countdown`：start/end/title 倒计时。
- `mg_quote`：quote + author 金句引用。
- `mg_benchmark`：title/specs{cpu,gpu,ram,ssd}/bars[] 性能对比柱状图。


### 4.4 接口（以服务端 /guide 文档为准）

| 能力 | 描述 |
|-----|------|
|  POST /mg/generate 提交 MG 模板渲染任务 |
|  GET /mg/status/{task_id} 查询进度并获取 output_url |
|  GET /mg/result/{task_id} 下载渲染完成的 MP4 |
---

## 五、垂类剪辑模板

### 5.1 电影解说

**模板结构**：
```
片头 (3s) → 开场悬念 (5s) → 剧情概述 (15s) 
→ 名场面 1 (10s) → 分析点评 (8s) → 名场面 2 (10s) 
→ 总结升华 (5s) → 片尾引导关注 (3s)
```

**AI 能力**：
- 自动识别电影名场面（高潮+转折）
- LLM 生成解说文案（悬念钩子 + 剧情 + 点评）
- TTS 配音（解说风格声音样本）
- 画面匹配解说文案时间轴

### 5.2 知识科普

**模板结构**：
```
问题引入 (5s) → 概念解释 (10s) → 案例演示 (10s) 
→ 数据支撑 (5s) → 实用技巧 (8s) → 总结 + 引导 (5s)
```

**AI 能力**：
- LLM 根据选题生成科普脚本
- 素材库中匹配知识类画面（产品/图表/示意图）
- 自动生成字幕 + 标注
- 字幕高亮关键词

### 5.3 通用模板引擎 + 导入

所有垂类模板统一用 JSON 描述结构，引擎可扩展。**支持从剪映/PR 导入模板**（参见 3.6 导入流程）：

```json
{
  "category": "movie_review",
  "name": "电影解说-3min",
  "duration": 180,
  "source": "built-in",       // built-in | imported | market
  "source_file": "",           // 原始导入文件名
  "segments": [...],
  "voice_profile": "解说_男声",
  "bgm_profile": "悬疑_紧张"
}
```

**模板来源**：
- `built-in`：系统预置
- `imported`：从剪映 .drt / PR .xml 导入
- `market`：从模板市场下载

### 5.4 接口（以服务端 /guide 文档为准）

| 能力 | 描述 |
|-----|------|
| 模板生成成片 | 根据模板类型 + 素材生成成片 |
| 模板列表 | 获取所有模板 |
| 模板校验 | 校验用户自定义模板 |

---

## 六、即梦定时任务

### 6.1 目标

在客户端内置定时调度器，让即梦（Dreamina）在夜间低峰时段自动批量执行 AI 图像生成任务。
> **状态（2026-08-01）**：🔴 未实现。`dreamina_page.py` 仅支持单次文生图，`scheduled_tasks_page.py` 尚未接入即梦队列。

### 6.2 使用场景

| 场景 | 说明 |
|------|------|
| 夜间跑图 | 白天积累 prompt 队列 → 夜间自动执行 → 早上验收 |
| 批量生成 | 一次提 50+ prompt，按队列逐个生成 |
| 定时避峰 | 避开白天 GPU/API 高峰，降低成本 |
| 素材预生成 | 根据产品库 / 模板需求预先生成素材 |

### 6.3 功能设计

```
┌─ 客户端定时调度器 ────────────────────────────────┐
│                                                    │
│  任务队列 (JSON 持久化)                              │
│  ┌─────────────────────────────────────────────┐   │
│  │ prompt | 尺寸 | 数量 | 风格 | 优先级 | 状态  │   │
│  │ "红色鼠标" | 1024x1024 | 5 | 写实 | high | 等待│  │
│  │ "白色键盘" | 1024x1024 | 3 | 电商 | med  | 完成│  │
│  └─────────────────────────────────────────────┘   │
│                       │                            │
│                       ▼                            │
│  调度器 (QTimer / schedule 库)                       │
│  ├─ 定时触发 (如每天 02:00)                         │
│  ├─ 逐个调用 dreamina.exe 或即梦 API                │
│  ├─ 自动保存到 outputs/dreamina_scheduled/          │
│  └─ 完成后记录日志 + 可选通知                       │
│                                                    │
└────────────────────────────────────────────────────┘
```

### 6.4 定时配置

| 参数 | 默认值 | 说明 |
|------|-------|------|
| 执行时间 | 02:00 | 可配多个时间点 |
| 重试次数 | 3 | 失败自动重试 |
| 并发数 | 1 | 串行避免 API 限流 |
| 自动关机 | 否 | 完成后是否关机 |
| 状态通知 | 否 | 完成后弹窗/邮件 |

### 6.5 实现方案

**方案 A：客户端内置调度（推荐）**
- 用 Python `schedule` 库或 Qt `QTimer`
- 任务队列存本地 JSON
- 直接调用 `dreamina.exe` 子进程
- 优点：零服务端依赖，离线可用

**方案 B：服务端调度**
- 服务端部署 cron/APScheduler
- 客户端提交队列到服务端
- 优点：客户端关机也能跑（需服务端常驻）

**建议先用方案 A，后扩展方案 B。**

### 6.6 UI 设计

资源配置页面新增「⏰ 定时任务」tab：
- 任务列表：prompt / 尺寸 / 数量 / 状态 / 操作
- 按钮：添加任务 / 导入 CSV / 立即执行 / 清空已完成
- 定时设置：执行时间 / 重试 / 自动关机开关
- 执行日志：实时滚动

### 6.7 接口（以服务端 /guide 文档为准）

| 能力 | 描述 |
|-----|------|
| 任务队列管理 | 提交/更新定时任务队列 |
| 状态查询 | 查询队列状态 |
| 手动触发 | 手动触发执行 |

---

## 七、分镜脚本服务端存储

### 7.1 目标

把「分镜脚本」从客户端本地文件存储迁移到服务端，与产品库 / 素材库保持同一架构：多端可见、可备份、可协作；「一键成片 → 脚本成片」直接读取服务端脚本列表。
> **状态（2026-08-01）**：🟡 接口需求已提交（保存/列表/读取/更新/删除 5 个接口），服务端 `/guide` 尚未出现接口，客户端仍走本地 JSON。

### 7.2 现状与问题

- **现状**：分镜脚本保存在客户端本地 `KNOWLEDGE_MEDIA_DIR/<选题>/storyboard/*.json`，保存时默认只导出 Excel，需手动选择 JSON 才会生成供脚本成片使用的文件（已临时用"保存时自动生成 JSON"缓解）。
- **问题**：
  1. 「一键成片 → 脚本成片」只扫描本地 `.json`，默认流程下看不到已保存脚本；
  2. 脚本不跨机器、不跨用户共享，无法统一备份；
  3. 与"产品库 / 素材库都在服务端"的架构不一致。

### 7.3 功能设计

- **保存**：分镜页「保存」→ 除本地 Excel / Markdown 导出外，同时把 JSON 脚本上传到服务端；
- **列表**：「一键成片 → 脚本成片」从服务端拉取脚本列表（脚本**多端共享**，不做机器码隔离）；
- **读取**：选中脚本后可直接提交服务端执行成片，不依赖本地文件；
- **管理**：服务端脚本支持更新与删除（分镜页可管理已保存脚本）；
- **兼容**：本地 JSON 保留为离线缓存 / 兼容来源，服务端不可达时回退本地扫描。

### 7.4 接口规划（服务端新增）

| 端点 | Method | 说明 |
|------|--------|------|
| `/api/storyboard/scripts` | GET / POST | 脚本列表 / 保存脚本 |
| `/api/storyboard/scripts/{id}` | GET / PUT / DELETE | 脚本读取 / 更新 / 删除 |

> 实际接口命名、参数、返回格式以服务端 `/guide` 或 OpenAPI 文档为准。

**提交与实现（2026-08-01）**：已按服务端 `/guide`「POST /interfaces/request 提交新接口需求」契约，将本章 CRUD 整理为可直接提交的负载 [分镜脚本服务端接口需求.json](分镜脚本服务端接口需求.json)（保存/列表/读取/更新/删除 5 个接口）。**提交结果（2026-08-01）**：服务端 `/interfaces/request` 已实现，5 条需求已全部提交成功（status=pending）：保存=REQ_A3CEB501，列表=REQ_D6F64EFD，读取=REQ_1BA7246F，更新=REQ_36570C3A，删除=REQ_D0532DC5（保存/列表已按“多端共享、不做机器码隔离”修正重提）。服务端实现后状态置 done 并补充 /guide，客户端再按 /guide 对齐。

**现状核对（2026-08-01）**：服务端 `/guide` 已有「分镜脚本成片」任务接口——`POST /scheduled/tasks`，`task_type=storyboard_montage`，`params.shots[]`（服务端自动搜素材→TTS→FFmpeg→质量评分），客户端提交后轮询 `/tasks/unified/{id}`；但「分镜脚本存储/列表」接口（`/api/storyboard/scripts`）尚未在 `/guide` 出现，本节存储部分仍为规划，待服务端实现后补充文档，客户端再对齐（当前客户端保存先走本地 JSON，服务端接口就绪后切换）。

### 7.5 客户端改动

- `storyboard_page`：`_save_storyboard` 增加服务端上传（`config_manager` 服务端地址 + `http_post`）；
- `compile_video_page`：`_scan_storyboard_scripts` 优先服务端列表，本地扫描作回退；
- 数据字段沿用现有 JSON 结构（`topic / ratio / total_duration / shot_count / shots / saved_at`）。

---

## 八、优先级 & 依赖

| 优先级 | 功能 | 依赖 | 预估 | 状态 |
|--------|------|------|------|------|
| P0 | 一键成片模板 + 素材匹配 + 模板导入(PR/剪映) | CLIP + jianying_exporter 已有 | 3 周 | 🟡 产品/脚本成片已通，模板结构及 PR/剪映导入未落地 |
| P1 | 剪映导出增强 + 审美判断 | jianying_exporter 基础已有 | 3 周 | 🟡 多片段时间轴/转场/字幕/BGM 已落地，审美判断未做 |
| P1 | 电影解说模板 | LLM + TTS + 素材匹配 | 2 周 | ⏳ 规划中 |
| P1 | 即梦定时任务 | dreamina.exe 已有 | 1 周 | 🔴 未实现（仅单次文生图） |
| P1 | 分镜脚本服务端存储 | 服务端脚本 CRUD 接口 + 客户端改造 | 1 周 | 🟡 接口需求已提交，待服务端实现 |
| P2 | 希区柯克 & 特效镜头 | ffmpeg 滤镜 | 1 周 | ⏳ 规划中 |
| P2 | 知识科普模板 | 同上 | 1 周 | ⏳ 规划中 |
| P2 | MG 动画整合 | Remotion 已有 | 1 周 | 🟡 客户端已就绪，建议服务端渲染并接入一键成片 |
| P2 | 模板市场 | 模板导入完成后 | 2 周 | ⏳ 规划中 |

---

## 九、接口汇总（服务端新增）

> **注意**：以下接口仅为功能规划参考，实际接口命名、参数、返回格式以服务端 /guide 或 OpenAPI 文档为准。客户端开发时需先查阅服务端最新文档再对接。

```
模板类：一键成片 / 模板列表 / 导入PR剪映 / 导出模板 / 模板市场
素材类：素材匹配 / 编译成片 / 导出剪映工程
评价类：审美评价 / 节奏卡点分析
MG动画：提交渲染 / 查询进度
定时任务：队列管理 / 状态查询 / 手动触发
融图类：产品图+背景融合
脚本类：分镜脚本保存 / 列表 / 读取 / 删除
```

---


## 十、封面制作模板化

> **设计理念（2026-08-02 对齐）**：封面制作使用**封面类模板（type=cover）**，与动效模板（type=motion）严格区分——
> 封面是「静态画面 + 参数驱动」，动效是「视频动效模板」。上传的模板**先进入图层编辑模式拆成图层处理**
> （支持 PSD 逐层解析），也可拆分为封面模板参数（标题/副标题/卖点/背景色/文字色/比例）驱动本地渲染。

### 10.1 目标

将封面制作页改造为「封面模板（type=cover）驱动」的流程，同时保留本地图层式编辑器作为高级微调入口。

### 10.2 当前状态（2026-08-02）

- 页面拆分为「图层编辑」+「封面模板」两个 tab：
  - **图层编辑**：本地 `QGraphicsScene` + `QPainter` 分层渲染（背景/主体/标题/副标题/卖点…），高级微调入口。
  - **封面模板**：模板库只展示**封面类模板**（统一模板接口 `GET /templates?type=cover`，兜底内置 cover 模板），
    右侧按模板 `params` 生成「封面参数」表单（标题/副标题/卖点/背景色/文字色/比例）；
    点「预览」把参数**拆到图层编辑器**并本地渲染 PNG，点「导出」输出 PNG。
- **上传模板（图片/PSD）**：上传后**先进入图层编辑模式**，把文件拆分为图层处理：
  - PSD：用 `psd_tools` 逐层解析，每个可见图层成为图片图层，并生成扁平预览图；
  - 普通图片：整图作为底层「模板参考」图层，可配合「模板→复刻构图」AI 自动排版。
- 已移除「上传视频」入口（封面制作不依赖视频素材）。

### 10.3 流程

```
上传模板（图片/PSD）→ 进入图层编辑（拆图层处理 / AI 复刻构图）
                                │
封面模板 tab：选封面模板(type=cover) → 填封面参数
                                │
          预览 = 参数拆到图层 → 本地 QPainter 渲染 PNG
                                │
          导出封面 PNG（写入素材库）
```

模板参数（type=cover）：
- 封面比例：9:16 / 16:9 / 1:1。
- 标题、副标题、卖点文案（支持多行）。
- 背景色、文字颜色。
- （可扩展）主体图片、字体、字号、位置、装饰元素、Logo、安全区提示。

### 10.4 接口

| 能力 | 接口 | 说明 |
|-----|------|------|
| 封面模板列表 | `GET /templates?type=cover` | 统一模板接口按封面类型过滤（服务端暂未提供 cover 模板，客户端用内置 cover 模板兜底） |
| 封面模板校验/保存 | `POST /templates/validate` / `POST /templates` | 统一模板接口（type=cover） |
| 预览/导出 | 本地 QPainter 渲染 | 参数拆到图层编辑器渲染 PNG；服务端提供 type=cover 渲染后，可改用 `POST /templates/render` + `GET /templates/render/result|download/{task_id}` |
| PSD 图层解析 | psd_tools（客户端内嵌） | 上传 PSD → 逐层拆分为图片图层 |

## 十一、界面自动化与操作减负（UX 改进）

> **设计原则（2026-08-02）**：让用户「少操作、阶段自动衔接、尽量自动化」：
> - 主流程一键化：能从上游自动带参，就不让用户重复选择/填写；
> - 提交即衔接：任务提交后自动进入监控页，不要求用户手动跳转；
> - 默认自动化：高频操作（任务轮询、参数预填）默认开启，可手动关闭。

### 11.1 第一批（已实施，2026-08-02，commit 2425409）

| 改进 | 说明 |
|------|------|
| 一键成片提交自动衔接 | 产品成片「开始执行」（立即执行）提交成功后，自动切换到「成片任务」页并开启自动刷新 |
| 成片任务自动刷新默认开启 | 有进行中任务时每 5 秒自动刷新列表（原默认关闭需手动勾选） |
| 模板主题自动预填 | 选择产品 / 应用成片模板后，自动把产品卖点首行填入模板 topic 参数（用户已填则不覆盖） |

### 11.2 后续批次（按优先级）

| 优先级 | 改进 | 说明 |
|--------|------|------|
| P0 | 素材检索 → 一键成片自动带参提交 | 选中素材并匹配产品后，一键直达成片执行（4 步 → 1 步） |
| P0 | 封面制作按产品自动填 | 封面标题/卖点自动带入当前产品信息 |
| P1 | 任务完成通知 | 成片/去字幕等任务完成时系统通知，无需盯屏 |
| P1 | 一键成片批量 | 一个产品 × 多个模板一次提交多片 |
| P2 | 模板收藏 / 最近使用 | 常用模板置顶，减少重复选择 |
| P2 | 跨页上下文 | 最近产品/素材自动带入各功能页 |

### 11.3 验收标准

- 从「素材检索」选中素材到成片提交，操作 ≤ 2 步；
- 提交任务后无需手动跳转即可看到进度；
- 默认路径下所有参数（产品/主题/素材）无需手动重复填写。
---

## 十二、客户端双模式改造（运营 / 剪辑）

> **目标（2026-08-08）**：当前客户端偏「专业剪辑师」工具视角（功能模块式侧边栏 + 参数密集型页面）。
> 参考主流智能体客户端（扣子 Coze / 即梦 / Dify / 剪映），改造为**「运营模式 + 剪辑模式」双角色可切换**的形态：
> 运营用户按「目标」使用智能体能力（一句话需求 / 任务卡片 / 极简向导），剪辑用户保留全部专业工具。
> 与「十一、界面自动化与操作减负」的关系：十一章是操作细节的渐进优化；本章是**产品形态级重构**，十一章 11.2 的 P0/P1 项并入本章 P0/P1 落地。

### 12.1 现状问题

| 现状 | 问题 |
|------|------|
| 侧边栏 20+ 功能入口按职能分组（方案脚本/媒体库/成片制作/视频运营/系统设置） | 运营用户不知道"做一条带货视频"该点哪个 |
| 智能混剪 4 步向导（分割→拼接→配音→合成）每步含专业参数（转场/LUT/BGM/字幕） | 需先懂"镜头、转场、音轨"等概念 |
| 一键成片已带模板，但仍是「选产品→选模板→填素材/文案/音频/封面 4 组参数」的表单流 | 把"做视频"拆成表格，步骤感强 |
| 界面按「怎么做」组织（工具视角） | 应改为按「要什么」组织（目标视角） |

### 12.2 双模式总体架构

```
登录/首次启动 → 选择角色（可随时在顶部切换）
                ├─ 运营模式（目标导向 · 默认给非专业用户）
                │    ├─ 智能体工作台首页：一句话需求 or 任务卡片
                │    ├─ 任务卡片 → 极简向导（只问必要参数，高级项折叠）
                │    └─ 结果中心：进度 / 预览 / 下载 / 发布 / 送入剪辑精修
                └─ 剪辑模式（专业工具）
                     ├─ 保留现有全部专业页面
                     ├─ 增强：时间线 / 多轨 / 关键帧 / 转场库 / 逐镜头精修
                     └─ 接收运营成片 → 送入智能混剪 / 剪映草稿精修
共享底座：素材库 / 模型配置 / 账户 / 产出目录 / 任务队列（切换角色只是视图变化，数据完全共享）
```

### 12.3 运营模式设计

#### 12.3.1 智能体工作台首页（默认首屏）

- 顶部一句话需求输入框：`帮我把这段文案做成带货视频，用老怀的声音，竖屏15秒`
- 高频任务卡片（点击进入极简向导）：

| 卡片 | 底层能力（已存在） |
|------|------|
| 一键成片 | `/scheduled/tasks`（脚本成片）、`/material/search`、本地 ffmpeg |
| 智能混剪 | `/montage/concat`、`/material/score_clip` |
| 直播切片 | 现有 live_clip 能力 |
| 声音克隆 | `/voxcpm/tts` |
| 封面制作 | 本地 ffmpeg + rembg + Dreamina |
| 营销检测 / 视频评价 | `/llm/chat/completions` + 本地抽帧 |

#### 12.3.2 极简向导（渐进式披露）

- 每个任务只问「必要信息」（如一键成片：产品或文案 → 时长/比例 → 生成）
- 高级参数（模板、配音音色、字幕样式、转场等）默认取合理值，放「更多设置」折叠区
- 从现有页面包装，不新造底层能力

#### 12.3.3 结果中心

- 统一展示所有任务进度（复用成片任务/任务队列页）
- 完成态动作：预览 → 下载 → 发布（待评估）→ 送入剪辑精修（→ 智能混剪 / 剪映草稿）

### 12.4 剪辑模式设计

- 完整保留现有侧边栏全部专业入口，默认首页换成专业工作台
- 增强方向（演进）：时间线可视化、多轨、关键帧、逐镜头精修、PR/剪映导出（剪映导出已支持多片段+转场）
- 运营↔剪辑衔接：运营成片一键「送入智能混剪 / 剪映草稿精修」

### 12.5 技术选型与实现方案

**决策：分阶段渐进，不整体重写。**

| 阶段 | 内容 | 技术栈 | 涉及模块 | 预估 |
|------|------|--------|---------|------|
| **P0** | 角色切换器 + 侧边栏按角色过滤 + 运营首页（任务卡片）+ 一键成片/声音克隆极简向导 | PySide6 Widgets + 现有 QSS 主题 | `main_window_sidebar.py`、`gui_main.py`、新增 `agent_home_page.py`、`compile_video_page.py`、`voice_clone_page.py` | 1 周 |
| **P1** | 运营首页升级为「智能体工作台」（对话输入、卡片动效、结果中心）+ 其余任务卡片极简向导 | 宿主 Widgets + 内嵌 `QWebEngineView`/`QQuickWidget` 承载运营首页（已有 CEF/QWebEngine 基础） | 新增 `agent_workbench`（Web/QML 页）、`scheduled_tasks_page.py` | 1-2 周 |
| **P2** | 自然语言智能体编排（LLM 意图识别 → 组装参数 → 调现有接口链）+ 一键发布（待评估） | 客户端 LLM 编排（`/llm/chat/completions`）+ 现有 worker 链 | 新增 `agent_orchestrator`、`compile_video_page.py`、平台接入 | 1-2 周 |

- 界面原型先用 HTML 高保真交互稿（`visualize` 输出）在对话中验收，确认后再落代码。
- 运营首页不做成纯 Widgets 硬堆（对话流/卡片动效成本高）；用内嵌 Web 单独承载一个可插拔首页，专业页面不动。

### 12.6 演进路线与依赖

| 里程碑 | 内容 | 依赖 | 验收 |
|--------|------|------|------|
| M1（P0） | 双模式骨架 + 运营首页任务卡片 + 极简向导 | 现有接口稳定；纯客户端 | 运营用户 2 步内发起一个成片任务 |
| M2（P1） | 智能体工作台 + 结果中心 + 全部任务卡片 | M1；内嵌 Web/QML 环境就绪 | 一句话需求可发起任务；任务结果统一预览/下载 |
| M3（P2） | 自然语言编排 + 一键发布 | M2；发布能力方向定案（服务端接口 or 客户端自动化） | 说一句话完成「成片→发布」闭环 |

### 12.7 服务端依赖结论

- **P0 / P1 纯客户端改造，服务端无需改接口**：复用的均为已存在且已对接的接口（`/voxcpm/tts`、`/scheduled/tasks`、`/montage/concat`、`/material/search`、`/llm/chat/completions` 等），接口契约以服务端 `/guide` 为准。
- **一键发布是唯一可能需服务端新增的能力点**：当前客户端无任何视频发布能力；需在 M3 前定案：① 服务端新增统一发布接口（合规、可扩展）或 ② 客户端浏览器自动化发布（复用现有浏览器采集技术栈）。
- 任务卡片覆盖范围受限处（如分镜脚本存储、模板市场等 PRD 标注「待服务端实现」的能力）在 P0/P1 暂不做或先做本地版。

### 12.8 验收标准

- 首次启动可选角色，顶部一键切换，切换后侧边栏/首页形态随之变化，数据不丢失；
- 运营模式下「一键成片」「声音克隆」等高频任务 ≤ 2 步发起，高级参数默认值可用；
- 结果中心可查看任务进度、预览、下载，并可一键送入剪辑精修；
- 剪辑模式保留全部现有功能，专业页面行为不变；
- 切换角色不产生重复数据/任务，底层素材库、产出目录、任务队列完全共享。
---

## 十三、多智能体编排（服务端智能体化）

> 服务端接口需求单：[多智能体编排服务端接口需求.md](多智能体编排服务端接口需求.md)（S1~S6，提交 /interfaces/request 用）

> **决策（2026-08-08）**：工作台「一句话需求」要真正"拆解任务→分配给不同智能体→统一管理任务"，需要两层前提：
> ① **能力注册表**（编排器必须知道服务端/客户端/外部各有多少智能体、各自输入输出）；
> ② **编排器本身是一个元智能体（Orchestrator Agent）**，由 LLM 驱动规划/路由/监督。
> **方向：服务端先行「智能体化」**——先由服务端提供能力注册表与编排执行能力，客户端编排器骨架随后对接。

### 13.1 目标

- 一句话需求 → 智能体拆解 → 分配给不同子智能体 → 统一任务管理（进度/暂停/取消/重试/人工确认）。
- 服务端成为"智能体工厂"：能力可注册、可编排、可被多端共享调度。

### 13.2 现状盘点：智能体/能力分层（客户端视角，权威以服务端 /guide 为准）

| 层 | 能力单元（示例） | 同步/异步 |
|---|---|---|
| 服务端接口 | `/llm/chat/completions`、`/voxcpm/tts`、`/whisper/transcribe`、`/material/search|list|scan|serve|thumbnail|distinct`、`/material/ocr`、`/material/score_clip`、`/montage/split|concat|beat`、`/vsr/remove`、`/scheduled/tasks`、`/tasks/unified/{id}`、`/comfyui/*`、`/health` | 同步 + 异步两类 |
| 客户端本地工具 | ffmpeg（切/拼/混音/转码）、剪映草稿导出、封面（ffmpeg+rembg+Dreamina）、直播切片 | 同步/本地任务 |
| 外部工作流 | 数字人（RunningHub/ComfyUI）、即梦（本地 CLI） | 异步/第三方 |

> 说明：**"智能体"≠ 仅服务端接口**。编排器必须把三层统一注册成一张能力目录。

### 13.3 能力注册表（Agent Registry）

每个能力单元的结构（schema）：

```json
{
  "id": "tts_voice_clone",
  "name": "声音克隆/TTS",
  "description": "输入文案+参考音频，返回克隆语音",
  "executor": "server",            // server | client_tool | external
  "api": "POST /voxcpm/tts",
  "input_schema": {"text": "string", "prompt_audio": "base64", "speaker": "string"},
  "output": {"audio": "file_ref"},
  "sync": true,                     // 同步返回 or 提交后轮询
  "needs_user_input": false,        // 是否需要人工确认/选素材
  "dependencies": []
}
```

- 权威来源：服务端 `/guide` 提供接口级能力；客户端补充本地工具 + 外部工作流，两边合并成一张注册表。
- 客户端启动拉取服务端注册表，本地 JSON 兜底（离线可用）。

### 13.4 编排器 = 元智能体（Orchestrator Agent）

由 LLM 驱动五个阶段：

```
用户一句话 → Orchestrator Agent：
  1. Plan    拆解为步骤 DAG（能力A → 能力B → 能力C，含依赖/参数）
  2. Route   按能力注册表匹配并分配给对应子智能体
  3. Execute 同步调用 / 提交异步任务
  4. Monitor 轮询 /tasks/unified、失败重试、状态聚合
  5. Confirm 遇需人工确认节点（选素材/确认文案/试听）暂停，等用户确认后继续
```

### 13.5 驱动策略与演进

| 阶段 | 执行地 | 说明 |
|---|---|---|
| Step 1（客户端先行） | 客户端 Orchestrator 调服务端单能力接口 | 快速验证；plan 用可序列化 JSON |
| Step 2（plan 标准化） | 同一份 plan 由服务端执行引擎读取 | 客户端执行器换成"提交 plan + 轮询" |
| Step 3（服务端终态） | 服务端 Agent 编排 | 客户端只做：提交/轮询/人工确认/展示 |

> **plan 是纯 JSON（步骤/参数/依赖/中间产物引用）**，Step 1 写的拆解逻辑在 Step 3 不重写，只换执行地。

### 13.6 服务端智能体化工作项（接口需求，按 POST /interfaces/request 提交）

| # | 需求 | 说明 |
|---|---|---|
| S1 | `GET /agent/registry` | 能力注册表：返回全部可编排能力（schema 见 13.3） |
| S2 | `POST /agent/tasks` | 提交 plan（或自然语言，服务端拆解），返回 `task_id` |
| S3 | 子任务模型 | 父任务 + 子任务树（`parent_task_id`），`/tasks/unified` 返回树状状态 |
| S4 | 人工确认节点 | 任务状态 `waiting_user_input`；`POST /agent/tasks/{id}/confirm` 继续 |
| S5 | 中间产物登记 | 子任务产物（音频/视频/图片）路径登记，供下游步骤引用 |
| S6 | 编排任务管理 | 列表/暂停/取消/重试；支持断点续跑（不依赖客户端在线） |

### 13.7 优先级与验收

| 里程碑 | 内容 | 依赖 | 验收 |
|---|---|---|---|
| S0 | 服务端能力注册表（S1）+ 子任务模型（S3） | /guide 扩展 | 客户端能拉到统一能力清单并展示"服务端有多少智能体" |
| S1 | 服务端编排执行（S2/S4/S5/S6） | S0 | 服务端可执行一份 plan，多端共享、断点续跑 |
| C1（客户端） | Orchestrator Agent 骨架 + 注册表合并 + plan 生成 | S0 可用或本地兜底 | 一句话"数字人口播带货视频"→ 自动拆解并逐个执行/轮询 |
| C2（客户端） | 编排任务管理页（进度/暂停/取消/人工确认） | S1 | 与工作台结果中心统一 |

> 状态：⏳ 规划中。用户已确认**服务端先行智能体化**；S0/S1 接口需求整理后走 `POST /interfaces/request` 提交服务端。