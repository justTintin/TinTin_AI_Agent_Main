# 螺丝钉-电商智能体矩阵 商业化 PRD v2.1

> **归属**：PRD 文档体系中的版本/专题需求文档（**客户端 V2 版本需求 = 客户端当前要实现版本**，2026-08-20 大怪定）；总纲与全局实现状态见 [PRD.md](./PRD.md)。

> 更新日期：2026-08-20（V2 口径确认） | 状态：Phase 2.5 已完成，**V2 为客户端当前要实现**，Phase 3+ 按 §13.4 客户端待接入清单推进

## 一、产品定位

面向电商带货商家的 AI 视频创作与运营平台。核心价值：AI 帮你策划→制作→复盘→涨粉，覆盖抖音到全平台。

**商业模式**：软件 License + 素材订阅制。本地负责轻量操作，重算力走私有服务端。

---

## 二、版本体系

```
基础版（付费）      直播切片 + 智能混剪 + 绑定1个抖音号
  └─ 标准版（进阶）  + AI分析 + 文案创作 + 一键成片 + 绑定3个平台账号
       └─ 旗舰版（全功能）+ 数据看板 + 全平台分发 + 数字人 + 工具类 + 绑定无限账号
```

### 账号绑定数量
| 版本 | 价格策略 | 可绑定账号数 | 说明 |
|---|---|---|---|
| 基础版 | 低价引流 | 1 个 | 仅抖音 |
| 标准版 | 主力产品 | 3 个 | 抖音 + 任选2平台 |
| 旗舰版 | 高客单价 | 不限 | 全平台（抖音/小红书/B站/快手/视频号/YouTube） |

### 功能映射（Feature Flag）
| Flag | 版本 | 功能 |
|---|---|---|
| `clip` | 基础 | 直播切片、智能混剪 |
| `copywriting` | 标准 | 知识库、产品资料、产品文案、分镜脚本、飞书选题 |
| `analytics` | 旗舰 | 数据看板、热点追踪、营销检测、视频预测 |
| `distribute` | 旗舰 | 全平台自动发布、格式适配、定时发布 |
| `digital_human` | 旗舰 | 数字人、声音克隆、视频修复、视频去字幕 |

**通用模块（所有版本）**：封面制作、图像抠图、智能分层、视频转文字、OCR、LUT调色、系统设置、素材检索、任务队列。

**注意**：素材管理（本地数据库+CLIP向量索引）已在 v2.0 中移除，改为服务端统一检索。

---

## 三、技术架构：混合模式（本地+服务端）

### 算力分布

```
┌─────────────────────────────────────┐
│  商家本地 PC（轻量）                 │
│  ┌───────────────────────────────┐ │
│  │  螺丝钉桌面端（PySide6）       │ │
│  │  ├ UI 交互 + 页面管理          │ │
│  │  ├ 素材浏览器（Electron）       │ │
│  │  ├ 镜头分割（scenedetect）      │ │
│  │  ├ ffmpeg 视频处理（分割/合成）  │ │
│  │  │                             │ │
│  │  └ 所有 AI 推理 → 服务端        │ │
│  └───────────────────────────────┘ │
│                    ↕ API            │
└─────────────────────────────────────┘
                     ↕ (HTTP API)
┌─────────────────────────────────────┐
│  私有计算服务器 192.168.111.18:8000  │
│  ┌───────────────────────────────┐ │
│  │  TinTin AI Service Platform   │ │
│  │  ├ Ollama 视觉模型             │ │
│  │  │  → 画面描述生成             │ │
│  │  ├ Whisper 语音转写            │ │
│  │  ├ VoxCPM 声音克隆/TTS        │ │
│  │  ├ CLIP 向量检索              │ │
│  │  │  → POST /material/search   │ │
│  │  ├ VSR 视频去字幕             │ │
│  │  ├ ComfyUI 工作流管理          │ │
│  │  ├ 任务队列管理               │ │
│  │  └ DeepSeek API（文案生成）     │ │
│  └───────────────────────────────┘ │
└─────────────────────────────────────┘
```

### 存储支持
- **本地素材**：支持本地磁盘目录直接扫描
- **NAS 素材**：支持 SMB/CIFS 网络共享挂载扫描
- 素材向量检索走服务端，客户端不存索引

---

## 四、License 系统重构

### 当前状态
- RSA 签名 License，硬件指纹绑定
- `features` 数组控制功能开关
- 文件式验证，无在线激活

### 需要改动
| 功能 | 优先级 | 说明 |
|---|---|---|
| 试用白名单 | P0 | 试用期仅指定手机号可激活，其他人拒绝 |
| 在线激活 | P0 | 启动时联网验证 License |
| 试用机制 | P0 | 首次安装 7 天全功能试用，无需 License |
| 功能开关 | P0 | 侧边栏/页面根据 `features` 显示/隐藏 |
| 续费提醒 | P1 | 到期前 7 天界面提示 |
| 离线宽容 | P1 | 断网时 72 小时内不锁死 |
| 防分享 | P2 | 机器码+使用频率异常检测 |

### 功能开关映射
```python
FEATURE_PAGE_MAP = {
    "clip": ["一键成片", "智能混剪", "直播切片"],
    "copywriting": ["知识库", "产品资料", "产品文案", "分镜脚本", "飞书选题"],
    "analytics": ["数据看板", "热点追踪", "营销检测", "视频预测"],
    "distribute": ["全平台发布", "定时发布"],
    "digital_human": ["数字人", "声音克隆", "视频修复", "视频去字幕"],
}
```

---

## 五、用户系统（P0）

### 接口
```
POST /api/auth/login          → 手机号+验证码登录
POST /api/auth/refresh        → 刷新 token
GET  /api/license/activate    → 激活 License
GET  /api/license/status      → 查询状态
POST /api/license/renew       → 续费
GET  /api/material/search     → 素材检索（服务端）
GET  /api/tasks               → 任务队列
```

---

## 六、素材订阅库（P1）

### 内容分类
- BGM（背景音乐，按风格/情绪/节奏）
- 模板（片头/转场/片尾/封面）
- 特效（粒子/光效/文字动画）
- AI 模型（Ollama 模型 / Whisper 模型）

### 功能
- 本地缓存机制（下载后离线可用）
- 版本更新提醒
- 收藏/搜索/预览

---

## 七、多平台分发（P2）

### 目标平台
抖音、小红书、B站、快手、视频号、YouTube

### 功能
- 视频格式自动适配（9:16 / 16:9 / 1:1）
- 标题/话题标签自动生成
- 定时发布（通过平台 API / 模拟）
- 发布状态追踪

---

## 八、数据看板（P1）

### 指标
- 播放量、完播率、互动率（点赞/评论/分享）
- 粉丝增长趋势
- 视频排名（按播放量/互动）
- 竞品对比

### 技术
- 云端存储分析结果（KB 级）
- 前端 ECharts 可视化
- 平台数据通过 API / Cookie 抓取

---

## 九、渐进路线

| 阶段 | 内容 | 状态 |
|---|---|---|
| **Phase 1** | 基础稳定版：素材浏览器、智能混剪流程打通、服务端部署 | ✅ 已完成 |
| **Phase 2** | 混合架构迁移：素材检索/去字幕/ASR/TTS 迁移到服务端 | ✅ 已完成 |
| **Phase 2.5** | 功能增强：卡点成片(一次上传+variant_count)、产品库迁移服务端、镜头评分服务端化、GPU自适应编码、镜头重组智能重排、LUT色彩还原、审美判断/视频评价预测 | ✅ 已完成 |
| **Phase 3** | ① 即梦定时任务排队 ② 模板场景标签梳理 ③ 数字人生成 A-roll ④ 部署 ComfyUI + 产品图生成 ⑤ 一键成片模板引擎 ⑥ MG动画整合 | ⏳ 下阶段（服务端大部分已就绪，客户端接入见 §13.4） |
| **Phase 4** | 剪映导出增强 + 垂类模板(电影解说/知识科普) | ⏳ 待开始 |
| **Phase 5** | 侧边栏版本过滤 + 账号绑定 + 支付 + 多平台分发 | ⏳ 待开始 |
| **Phase 6** | 云数据看板 + SaaS 后台 | ⏳ 待开始 |
| **Phase 7** | License 在线激活 + 功能开关 + 登录页 + 试用白名单 | ⏳ 待开始 |

---

## 十、技术约束

- **Python 3.11+** / PySide6 6.6.3 / Qt6
- **计算服务器**：TinTin AI Service Platform（192.168.111.18:8000）
- **DeepSeek API** 文本生成（文案/改写/匹配）
- **Ollama** 服务端视觉推理（qwen2.5vl 或更高）
- **Whisper** 服务端语音转写
- **VoxCPM** 服务端声音克隆
- **ffmpeg + scenedetect** 本地视频处理
- **Git + Gitea** 代码管理（jckunji.com:3000）
- **Windows 专用**运行平台
- 最低硬件：GTX 1060 6GB + 16GB RAM（本地只跑 ffmpeg+UI）

---

## 十一、AI 性能优化

### 服务端负责
| 任务 | 服务 | 说明 |
|------|------|------|
| 画面描述生成 | Ollama 视觉模型 | 多帧抽帧分析 |
| 语音转文字 | Whisper | 支持多语言/说话人分离 |
| 声音克隆 | VoxCPM | 文本→语音合成 |
| 素材检索 | CLIP 向量引擎 | 文本/图片搜素材 |
| 视频去字幕 | VSR | 上传→处理→下载 |
| 文案生成 | DeepSeek API | 电商带货文案 |

### 本地负责
| 任务 | 工具 | 说明 |
|------|------|------|
| 镜头分割 | scenedetect + ffmpeg | 纯 CPU，轻量快速 |
| 视频合成 | ffmpeg xfade | 6个以内视频，16GB 内存 |
| UI 交互 | PySide6 | 用户界面 |
| 素材浏览 | Electron | 独立进程 |

---

## 十二、当前版本技术债 & 待实现功能

| 问题/需求 | 严重程度 | 说明 |
|------|----------|------|
| 素材浏览器功能不全 | 🔴 | 只能浏览，缺搜索/筛选/拖拽/批量操作 |
| 一键成片模板引擎 | 🔴 | 预置模板结构 + AI 素材匹配 + 自动填充（Phase 3 核心） |
| 模板导入(PR/剪映) | 🔴 | 解析 .drt/.xml 工程文件转为内部模板（Phase 3） |
| MG 动画未 AI 驱动 | 🔴 | 4个模板手动填参，需整合到一键成片流程 |
| 即梦素材下载未入库 | 🔴 | CLI 能下载但未自动导入素材管理 |
| 视频去字幕效果不佳 | 🔴 | 当前 VSR 去字幕质量不达标，需优化算法/更换方案 |
| 视频水印功能 | 🟡 | 成片添加品牌水印（图片/文字），支持位置/透明度/动效配置 |
| License 在线激活未实现 | 🔴 | 当前仅文件式验证（Phase 7 最后） |
| 分镜脚本无法绑定素材 | 🟡 | 生成的脚本与素材库割裂 |
| 生成脚本后关联素材 | 🟡 | 脚本与源镜头缺显式关联（需求#20） |
| 打包排除客户数据 | 🟡 | pack_release.py 应排除客户业务数据 |

---

## 十三、下阶段主要任务（Phase 3）

> 状态：规划中 | 六大优先事项

---

### 13.1 即梦定时任务排队（P0）

**目标**：优化即梦（Dreamina）处理流程，支持任务队列 + 定时调度，夜间低峰自动批量执行。

**现状**：
- `dreamina_page.py`：已实现基础文生图（单次提交 + 轮询下载）
- `scheduled_tasks_page.py`：已实现服务端成片任务监控
- 缺少：即梦专属队列管理、定时触发、批量排队、失败重试

**任务分解**：

| # | 任务 | 说明 | 预估 |
|---|------|------|------|
| 1 | 即梦任务队列 | 本地 JSON 持久化队列（prompt/尺寸/数量/风格/优先级/状态） | 2d |
| 2 | 定时调度器 | QTimer/schedule 定时触发（可配多时间点），串行执行避免限流 | 2d |
| 3 | 批量执行引擎 | 逐个调用 dreamina CLI，失败自动重试(3次)，断点续传 | 2d |
| 4 | UI 集成 | 即梦页新增「队列」tab：添加/导入CSV/立即执行/定时设置/日志 | 2d |
| 5 | 结果入库 | 生成图片自动导入素材管理（解决“即梦素材未入库”技术债） | 1d |

---

### 13.2 模板场景标签梳理（P0）

**目标**：梳理并定义不同场景的模板标签体系，为后续素材智能匹配提供基础。

**现状**：
- PRD_V2.2 已定义模板 JSON 结构（含 tag 字段）
- 尚未落地实现，标签无统一规范

**任务分解**：

| # | 任务 | 说明 | 预估 |
|---|------|------|------|
| 1 | 场景分类定义 | 梳理电商带货/产品评测/品牌故事/活动促销/电影解说/知识科普等场景 | 1d |
| 2 | 标签体系设计 | 每个场景的 slot 类型 + 标签词表（如 hook/feature/cta/intro/outro） | 2d |
| 3 | 模板 JSON 规范 | 定义 `.tpl.json` 格式（slot/tag/duration/bgm/transition） | 1d |
| 4 | 预置模板制作 | 按场景制作 6+ 套模板（电商-15s/30s/60s、评测、品牌、促销） | 2d |
| 5 | 标签-素材映射规则 | 定义 tag → CLIP 检索 query 的转换规则 + 评分权重 | 1d |

---

### 13.3 数字人生成 A-roll（P0）——服务端已就绪，客户端接入

**目标**：用数字人技术生成 A-roll（主画面口播视频），作为成片的主轨道素材。

**现状（2026-08-20 更新）**：
- 服务端已就绪：`/workflow/execute`（数字人·图片驱动 2085292185062297602 / 视频对口型 2090001865487503362）
  + `POST /digital-human/batch`（多段文案批量 A-roll，submit=true/false 先备后提）
  + 本店音色 `voice_reference_path`（VoxCPM2 声音克隆）
- 客户端已有基础 UI（人物图片 + 驱动语音 → 生成）

**客户端任务分解**：

| # | 任务 | 说明 | 预估 |
|---|------|------|------|
| 1 | 数字人批量入口 | 调用 `/digital-human/batch`：多段文案输入 + 本店音色参考 + submit 开关；展示批次任务列表/进度（GET /digital-human/batch/{id}） | 2-3d |
| 2 | A-roll 与模板对接 | 数字人视频作为模板 slot 的主轨道，B-roll 素材叠加（服务端模板 `shoots[]` 已支持 slot 标签） | 3d |
| 3 | 视频对口型入口 | 已有视频 + 新旁白 → `/workflow/execute` type=对口型（保留原画面只改嘴型） | 1d |

---

### 13.4 客户端待接入清单（服务端 V2 已全部就绪，2026-08-20）

> **本清单为客户端 V2 待接入项统一入口**，后续客户端版本（V2.2+）需求沿用/迁移更新，以本清单为准；
> PRD-V2-3（素材库瀑布流）已取消（2026-08-09），不迁移至此。
> **V2 服务端已收官**，以下均为**客户端接入/界面**工作（合计约 7-11 天），接口均已就绪。

| # | 客户端项 | 服务端接口（已就绪） | 说明 |
|---|---------|---------------------|------|
| 1 | 音频库页面 | `GET/POST /audio/library*`、`/audio/bgm/tags`、`/audio/bgm/upload`、`/audio/beatmap`、卡点成片 `audio_id` | 选曲/分类/试听(Range)/上传 BGM 三维标签；卡点取曲 |
| 2 | 音效库页面 | `GET /sfx/library`（?category/tag）、`/sfx/analyze*`（PANNs 自动标注）、`/audio/gen/sfx` | 音效浏览/试听/分类；AI 音效生成入口 |
| 3 | AI 音频生成入口 | `/audio/gen/bgm`（MusicGen，style=auto 回流）、`/audio/gen/sfx`（AudioLDM2） | 音频库页内生成 BGM/音效并入库 |
| 4 | 插件下载页面（F3c） | `POST /material/import_plugin`、`/material/list?source=plugin`、`/material/stats?group=source` | 来源筛选/插件分类树/下载自动入库 |
| 6 | **成片任务打分反馈（内嵌，不建单独工作台）** | `GET /evaluate/by-task/{task_id}`（按成片任务取评价+维度）、`POST /evaluate/feedback/dimensions`（逐维改分提交） | 在成片任务详情页内嵌"打分反馈"区：播放预览 + 维度分数对照 + 改分提交（驱动通道A/B + guidance 回流） |
| 7 | 仿爆款入口 | `/viral/clone/*`（analyze/plan/pipeline/montage/review）+ `/workflow/execute` | 替换法流程 UI：拆解→复刻→管道→组装→对比 |

> **运营角色界面（数据看板/发布管理/审核工作台）不属 V2**——归 **V3**（PRD-V2-2 客户端 V3 需求 + V3-R1 复盘工作台，服务端 /ops/* 已就绪，客户端待 V3 实现；见 PRD.md 远期需求表）。

---

### 13.5 部署 ComfyUI + 产品图生成（P0）

**目标**：服务端部署 ComfyUI，跑通「产品 → 图」能力（产品图片生成/背景替换/场景化），为数字人和模板引擎提供底层图像生成能力。

**现状**：
- `apps/comfyui/`：已有 ComfyUI 源码（已从 git 移除，需重新部署）
- 数字人页已用 ComfyUI 后端（人物图片+音频→视频）
- 缺少：服务端正式部署、产品图工作流、客户端产品图生成入口

**任务分解**：

| # | 任务 | 说明 | 预估 |
|---|------|------|------|
| 1 | 服务端部署 ComfyUI | 服务器安装 ComfyUI + 模型（SDXL/Flux），配置 API 服务 | 2d |
| 2 | 产品图工作流 | 制作产品图生成工作流（产品抠图→背景生成/场景化/光影调整） | 3d |
| 3 | 客户端产品图页面 | 新增「产品图生成」入口：上传产品图 → 选场景/风格 → 提交生成 → 下载结果 | 3d |
| 4 | 批量生成 + 队列 | 多产品/多场景批量提交，复用即梦队列架构 | 2d |
| 5 | 与产品库联动 | 从产品库选取产品图 → 一键生成多场景产品图 → 回存产品库/素材库 | 2d |

---

### 13.6 一键成片模板引擎（P0）

**目标**：基于 13.2 标签体系，实现素材智能匹配 + 一键编译成片 + 模板导入(PR/剪映)。

**依赖**：13.2 标签体系、CLIP 向量检索(已有)、jianying_exporter(已有)、/montage/beat(已有)

**任务分解**：

| # | 任务 | 说明 | 预估 |
|---|------|------|------|
| 1 | 素材智能匹配引擎 | 解析模板 tag → CLIP 检索 → top5 候选 → 评分去重 → 自动填充 | 4d |
| 2 | 一键编译成片 | 模板 slot + 匹配素材 → ffmpeg 合成（含转场/BGM/字幕） | 3d |
| 3 | 模板导入（剪映/PR） | 解析 .drt / .xml → 转换为内部模板 JSON → 存入模板库 | 4d |
| 4 | UI 集成 | 一键成片 tab 新增模板选择/预览/素材替换/生成流程 | 3d |
| 5 | 服务端接口对接 | `/template/generate`、`/montage/match`、`/template/import` | 2d |

---

### 13.7 后续衍接（Phase 4+）

- 剪映导出增强 + 垂类模板扩展（电影解说 / 知识科普）
- 多平台分发 + 支付接入
- License 系统在线激活 + 功能开关（最后阶段）
