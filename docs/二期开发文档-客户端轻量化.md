# 螺丝钉-电商智能体矩阵 · 二期开发文档（客户端轻量化）

> 分支：`V1.0.7.22` ｜ 周期：2026-08-11 ~ 2026-08-13 ｜ 提交：4 个 ｜ 改动：35 文件（+5619 / -1407）
>
> 关联文档：`客户端轻量化-模型调用服务端化方案.md`、`客户端升级方案对比与选型.md`、`服务端蓝绿发布与升级方案-交付服务端团队.md`、`服务端OCR接口扩展需求-交付服务端团队.md`、`SERVER_API.md`

---

## 一、二期概述

### 1.1 目标

一期完成了核心功能搭建；二期的核心目标是 **客户端轻量化** —— 把原本内置在客户端、体积庞大的本地 AI 推理引擎（抠图 rembg/U2Net、VSR 去字幕、PaddleOCR 等）统一迁移到算力服务端执行，客户端只保留 UI 与轻量预处理，并同步建设 **客户端-服务端协同升级体系**。

### 1.2 核心成果

| 指标 | 结果 |
|------|------|
| 本地 AI 引擎服务端化 | 抠图 / 混剪描述 / 分镜检索 / VSR 去字幕 / OCR 共 5 类能力全部转远程 |
| 可削减客户端体积 | **约 34.4 GB**（`apps/vsr-v1.1.1` 12.8GB + `apps/vsr-v1.4.0` 12.1GB + `apps/PaddleOCR` 9.5GB），另 `apps/rembg` 0.2GB 待移除 |
| 新增服务端客户端封装 | `montage_client.py` / `matting_client.py` / `vsr_client.py` / `ocr_client.py` |
| 升级体系 | 客户端升级方案 + 服务端蓝绿部署方案 + 版本协商机制（三份方案文档） |
| 死代码清理 | 两个去字幕页面合计移除 400+ 行本地 Worker / 回调 / 安装器 |
| 联调验证 | OCR 三端点、VSR 端点均走真实客户端代码路径联调通过 |

### 1.3 技术原则

- **客户端-服务端分离**：客户端仅做 UI 与轻量预处理，AI 推理统一走 `compute_server_url` 指向的算力服务端
- **统一任务模式**：异步能力沿用「上传 → 轮询 `/tasks/unified/{id}` → 下载 `/xxx/download/{filename}`」三段式
- **地址单一来源**：所有服务端地址从 `ai_config.json` 的 `compute_server_url` 派生
- **向后兼容**：服务端未就绪的字段客户端可降级，不报错

---

## 二、Git 提交记录

| 提交 | 时间 | 说明 | 规模 |
|------|------|------|------|
| `fb54640` | 08-11 18:24 | 客户端轻量化修改（抠图/混剪/分镜服务端化 + 方案文档） | 23 文件，+3575/-120 |
| `d22724e` | 08-11 19:01 | 客户端轻量化修改二期（VSR 去字幕服务端化） | 6 文件，+323/-70 |
| `9b395c0` | 08-12 18:24 | 客户端、服务端的升级方案 + 部分 bug 修改 | 5 文件，+1334/-11 |
| `b35cd69` | 08-13 14:59 | 客户端轻量化改造（OCR 服务端化 + 死代码清理） | 8 文件，+420/-1239 |

---

## 三、核心主线：客户端轻量化（AI 引擎服务端化）

### 3.1 抠图 / 混剪描述 / 分镜检索服务端化（P1 + P2）

- 新增 `utils/montage_client.py`：`describe_shots()` 走 `/material/score_clip` 批量生成镜头描述
- 新增 `utils/matting_client.py`：抠图改为上传服务端 `POST /material/matting`，`RembgWorker` 不再本地加载 rembg/U2Net（抠图页与封面制作共用）
- storyboard 相似度检索 / 自动绑定素材改调服务端 `/material/search`（移除 `search_by_text` 死代码，消除 NameError）
- 修复 `BatchGenerateDescriptionsWorker` 引用未定义 `url` 的 NameError，LLM 调用简化为 `llm_chat_messages`
- **文件**：`montage_client.py`、`matting_client.py`、`image_matting_page.py`、`storyboard_page.py`、`montage/workers/desc_workers.py`

### 3.2 VSR 去字幕服务端化（P3）

- 新增 `utils/vsr_client.py`：`vsr_remove_remote()` 封装「上传 `/vsr/remove` → 轮询 → 下载 `/vsr/download/{filename}`」并落盘本地（原名_no_sub.mp4）
- 两个去字幕页面由「本地 subprocess 调 VSR 算法包」改为**统一走服务端**，移除「使用服务端」复选框改为提示文案，本地 Worker 分支删除
- v14 页多选区转 `sub_areas`，`sttn → sttn_auto` 映射
- 服务端任务提交后无法本地终止，「停止」按钮改为提示
- **文件**：`vsr_client.py`、`subtitle_removal_page.py`、`subtitle_removal_page_v14.py`

### 3.3 OCR 服务端化（P4）

- 重写 `utils/ocr_client.py`：新增 `video_ocr_remote()` / `image_folder_ocr_remote()` / `ocr_image_roi()`
- 视频 OCR：上传视频 → 服务端逐帧抽样 OCR + ROI 框选 + 数字过滤 → 下载 CSV
- 图片文件夹批量 OCR：客户端打包 zip 上传 → 服务端关键词锚点定位提取 → 下载 CSV/txt
- 两个 OCR 页面 Worker 改服务端；环境检查页 OCR 改「远程模式」；移除 PaddleOCR 一键安装器与 `PADDLEOCR_*` 路径常量
- **文件**：`ocr_client.py`、`video_ocr_page.py`、`image_folder_ocr_page.py`、`env_config_page.py`、`main_window_installers.py`、`paths.py`

### 3.4 死代码清理

- 移除两个去字幕页面的本地 Worker 类（`SubtitleRemovalWorker` / `SubtitleRemovalWorkerV14`）、`on_worker_*` 回调、`poll_preview_image`、`import gc`
- 移除 PaddleOCR 安装器死代码（`PaddleOcrInstallWorker` 等）
- `subtitle_removal_page.py` 1057→920 行，`subtitle_removal_page_v14.py` 1391→1115 行
- 保留 `cleanup_ui` / `self.timer` 通用 UI 复位辅助

### 3.5 轻量化收益

| 本地引擎 | 目录 | 体积 | 状态 |
|---------|------|------|------|
| VSR 去字幕 v1.1 | `apps/vsr-v1.1.1-windows-nvidia-cuda` | 12.8 GB | 已服务端化，可删 |
| VSR 去字幕 v1.4 | `apps/vsr-v1.4.0` | 12.1 GB | 已服务端化，可删 |
| PaddleOCR | `apps/PaddleOCR` | 9.5 GB | 已服务端化，可删 |
| rembg/U2Net | `apps/rembg` | 0.2 GB | 已服务端化，待移除 |

> ⚠️ 删除前建议先在 GUI 用真实文件把去字幕、OCR、抠图各页面实测一遍，确认服务端链路稳定后再删目录。

---

## 四、升级体系建设

### 4.1 客户端升级方案（`客户端升级方案对比与选型.md`）

- 对比 8+3 套升级方案（全量 ZIP / 文件增量 / 二进制差分 / 双槽位 / 安装包等）
- 推荐主线：**服务端驱动**（方案 I 版本协商 + 方案 J 服务端蓝绿 + 方案 C 全量 ZIP），阶段 2 叠加文件级增量
- 核心机制：服务端先升级 → 客户端交互时发现版本不匹配 → 服务端下发更新

### 4.2 服务端蓝绿部署（`服务端蓝绿发布与升级方案-交付服务端团队.md`）

- Docker + nginx blue/green upstream 切流，原子升级 + 秒级回滚
- `/health` 分级状态（ready/warming/down）驱动切流；alembic 前向兼容迁移
- 模型权重 / 配置 / 数据全 volume 隔离不进镜像；GPU 显存约束用串行预热 + 滞后销毁

### 4.3 版本协商机制

- 服务端维护 `compatibility.json`（`min_client_version` / `recommended_client_version`）
- 客户端每次 API 调用携带 `X-Client-Version` 头
- 服务端中间件三态响应：HTTP 426（强制更新）/ `X-Update-Available: soft`（软提示）/ 正常
- 覆盖冷启动、运行中、服务端升级瞬间三大场景

---

## 五、智能混剪功能增强

| 项 | 说明 |
|----|------|
| 分割镜头综合评分 | `_score_clip()` 5 维度 0~10 分，表格/预合成详情显示，≥7 自动勾选 |
| 镜头去重择优 | 帧哈希 + 汉明距离去重，清晰度/对比度/音频质量分择优 |
| LUT 色彩还原 | 系统设置「资源配置」管理 LUT 映射，拼接时应用 `lut3d` 滤镜 |
| 主要产品提示词 | 步骤 1 新增产品提示词输入，分割后自动触发围绕该产品的镜头分析 |
| 分镜头定点预览 | ffplay `-ss/-t` 从镜头起点直接预览，无需从头播完 |
| 按文案智能匹配 | 新增「AI 生成文案」按钮，镜头重组按钮位置调整 |
| 灰片/Log 检测 | `detect_log_video()` 色彩元数据 + 帧对比度分析 |

---

## 六、稳定性与 Bug 修复（节选）

| Bug | 根因与修复 |
|-----|-----------|
| LLM 调用无响应 | `/llm/chat` → `/llm/chat/completions`（服务端实际端点） |
| 任务队列页卡顿崩溃 | Worker 子线程直接操作 Qt 控件 → 拆分数据获取与 GUI 更新 |
| 镜头重组按钮失效 | `_build_precompose_plans` 缺 `def` 声明行，信号槽静默吞错 |
| AI 文案/改写按钮失效 | Worker 构造传未定义 `api_url`/`api_key` → 改传空串走 `llm_chat` |
| 试听配音不能暂停 | 未处理 PausedState + Windows 路径正反斜杠比较失效 → 三态切换 + normpath |
| 最终合成卡死 | BGM 在移动盘 I/O 阻塞 → 预拷贝本地 + ffmpeg 卡死看门狗 + 超时 |
| 步骤3配音混入残留视频 | 整目录扫描回退 → 明确文件列表传入 + 去重过滤 |
| 服务端健康灯掉线 | `/ollama/status` 同步阻塞事件循环 → machine_id 缓存 + async 化 |
| VSR 下载 404 | 服务端文件名在 `params.output` 字段，补充读取 |

---

## 七、服务端配套接口

### 7.1 已落地（联调通过）

| 端点 | 能力 | 模式 |
|------|------|------|
| `POST /material/matting` | 图片抠图 | 同步 |
| `POST /material/score_clip` | 镜头评分/描述（支持 product_prompt） | 异步 |
| `POST /material/search` | CLIP 语义检索 | 同步 |
| `POST /vsr/remove` | 视频去字幕 | 异步 |
| `POST /ocr/image` | 单图 OCR（可选 ROI） | 同步 |
| `POST /ocr/video` | 视频逐帧 OCR | 异步 |
| `POST /ocr/batch` | 图片文件夹批量 OCR | 异步 |

### 7.2 交付服务端团队的文档

- `服务端蓝绿发布与升级方案-交付服务端团队.md`：Docker 蓝绿部署落地
- `服务端OCR接口扩展需求-交付服务端团队.md`：OCR 三端点契约（参数表 / CSV 格式 / 关键词定位算法）

---

## 八、验证与联调记录

- **OCR**：`/ocr/image`（整图 + ROI）、`/ocr/video`（逐帧抽样 + 变化文本 + CSV）、`/ocr/batch`（zip + 关键词锚点提取）全部走真实 `ocr_client` 代码路径联调通过
- **VSR**：PIL 渲染带文字字幕视频 → 上传 → STTN 擦除 → 下载，全链路约 12 秒通过；无字幕场景服务端正确返回「未检测到字幕」并携带日志（错误回传链路正常）
- 所有改动文件 `py_compile` 通过、IDE 无错误、无残留引用

---

## 九、遗留事项与后续规划

- [ ] **删除本地引擎目录**：实测确认后删除 `apps/vsr-*`、`apps/PaddleOCR`、`apps/rembg`（合计约 34.6 GB）
- [ ] **服务端 VSR 引擎崩溃修复**（需求 #29）：`/vsr/remove` 执行即失败，需服务端排查依赖/模型权重/GPU
- [ ] **生成脚本关联素材**（需求 #30）：为口播文案建立与源镜头/素材的显式关联，支持双向反查
- [ ] **升级体系落地**：实现 `studio/updater/` 模块 + 服务端 `/api/updates/check` + 版本协商中间件
- [ ] **`python_embeded` 精简**：13 GB 嵌入式 Python 环境中可裁剪的依赖排查
