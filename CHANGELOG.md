# 版本日志

## v2.1.0 (2026-06-30)

### 系统配置重构
- 「⚙️ 环境配置」拆分为 5 个独立侧边栏菜单，每个内部用 Tab 组织子模块：
  - **🖥️ 运行环境**：Python/GPU/CUDA 检测 + 硬件自动优化 + 一键修复 + 备份还原 + Python 终端
  - **🔌 平台接入**：ComfyUI 启停/测试 + RunningHub API + 即梦 + 飞书配置
  - **🧠 模型配置**：Ollama / VoxCPM / Whisper / CLIP / PaddleOCR / rembg — 每个模型独立 Tab，含启动/停止/测试/下载
  - **📊 系统信息**：硬件信息 + 系统日志 + 帮助文档 + 素材目录
  - **📦 资源配置**：声音样本 + 素材目录（支持多目录、NAS 入库）
- 合并冗余菜单：AI 设置→平台接入、大模型配置→模型配置、系统日志→系统信息、备份还原/Python终端→运行环境
- `env_config_page.py` 拆为 `EnvRuntimePage / ComfyUIConfigPage / ModelConfigPage / SystemInfoPage` 4 个独立页面类

### 跨平台适配
- README 重写为完整跨平台文档（Linux/Windows），新增 `docs/DEVELOPMENT.md`
- 素材目录平台默认值：Linux 自动含 `/mnt/nas/Photos`，Windows 仅本地路径
- NAS 路径提示按平台区分（`\\192.168.xxx` vs `//192.168.xxx 或 /mnt/nas`）
- `config/paths.py` 新增 `MATERIALS_PLATFORM_DEFAULTS`

### 修复
- 素材浏览器 Electron 依赖修复：`node_modules/electron` 缺失二进制
- `.gitignore` 添加 `node_modules/` 防止万级文件误提交
- CLIP 混合搜索优化：关键词字段权重分级，过滤低质向量结果
- 侧边栏双击预览：委托事件替代内联属性

## v2.0.0 RC (2026-06-23)

### 视频AI智能重命名
- 修复批量「分析并预览」崩溃：后台 worker 缺少 `_folder_path`，分析在调用 AI 之前就抛 AttributeError 退出，导致无抽帧、无分析结果、文件名只剩路径占位。改为构造函数传入 `folder_path`
- 修复假完成状态：原先无条件把残留"分析中"刷成"已分析 ✓"并显示"分析完成"，现按真实结果统计成功/失败/未完成
- 说明：deepseek-chat 为纯文本模型不触发抽帧，需配置视觉模型（qwen-vl/glm-4v/gpt-4o 等）才会按画面识别

### 智能混剪与批量视频制作
- 修复「未检测到镜头切点」分支报错：`finished` 信号定义为 3 参数但该分支只 emit 2 个，导致 TypeError；补齐参数
- 0 镜头时弹窗提示调低分割阈值，不再误显示"切出 0 个镜头"并提前返回
- 新增「📚 批量分割全部」：对列表所有视频依次镜头分割+重命名（不触发自动转写/描述）
- 新增「🌟 批量挑精华片段」：每个视频按"清晰+适度运动"评分挑出一段最佳片段（时长可调，默认 5 秒）
- 精华片段统一写入共享 `splits` 目录（同型号视频便于组合混剪），序号递增互不覆盖，生成后自动刷新下方镜头列表
- 输出画幅新增「与原视频一致」选项并设为默认（按第一个素材分辨率输出）
- 已生成排列视频列表每行新增「✍ 生成文案」按钮：输入品牌/产品/型号，调用大模型（deepseek）生成口播文案并存为视频同名 .txt，下一步「克隆口播配音」自动载入；已有文案时按钮变「重新生成」并二次确认覆盖
- 文案改为「按画面生成」：组合排列后，每条视频读取自身 `_sources.txt` 的镜头画面描述，由大模型据此逐镜头生成口播文案（每行对应一个画面），同型号产品共用一份背景信息（首次填写后全局复用）
- 已生成排列视频列表上方新增「✍ 一键根据画面生成全部文案」按钮：填一次产品背景后，串行为所有组合视频按各自画面批量生成口播文案；已有文案时可选「全部覆盖 / 只补缺失 / 取消」，完成后汇总成功与失败项
- 修复克隆配音「远程主机强迫关闭连接 (WinError 10054)」直接中断：VoxCPM 服务在生成中途崩溃/重置连接时，客户端改为最多重试 3 次，重试前轮询 `/health` 等待服务恢复，503(显存不足) 同样重试；单条请求超时从 120s 放宽到 180s
- 修复克隆配音「接口返回错误 (500): generator raised StopIteration」：该错误源于 VoxCPM 对过长文本单次生成时内部 `next()` 抛 StopIteration。现整体合成失败时自动「分句合成 + 拼接」兜底（句间补 0.15s 静音，跳过纯标点片段），短句几乎不触发该问题
- 克隆配音单条失败不再中断整批：失败项被记录并跳过，继续合成其余视频；结束后弹窗汇总成功/失败明细（全部失败才作为整体错误报出）

## v1.3.0 (2026-06-17)

### 界面优化
- 步骤3表格从4列改为2列，状态和操作按钮整合到每条数据第一行右侧
- 每条数据操作区新增 ▶ 播放原视频 和 🎵 选择背景音乐按钮
- 恢复各步骤界面的滚动条（QScrollArea），确保底部按钮和进度条可见
- 排列镜头数量下拉框添加暗色主题样式，修复白色背景不显示文字
- 排列镜头数量默认值改为扫描到的全部镜头数
- 移除步骤2中不用的功能：新文案匹配输入框、打开排列视频目录、排列文案显示、保存/导出按钮
- 移除步骤4中无功能的标题标签和计数标签

### 声音克隆修复
- 多行文案不再拆成逐句 API 调用（之前 N 行 → N 次 API → VoxCPM 服务质量递减），改为用中文句号连接一次调用
- 每次 TTS API 调用后添加 0.3 秒延迟，给服务端释放 GPU 状态
- 文案中换行自动转为中文句号断句，保持自然停顿

### 字幕优化
- 字幕按换行拆分为独立时间片段显示，不再全部堆在一起
- 字号从固定 14px 改为视频高度自适应（fontsize=h*0.025）
- 底边距从 h*0.02 调整为 h*0.06
- 背景框边距优化

### 配音功能增强
- 新增每条数据 🎬/🎙 切换：以视频长度为准 vs 以音频长度为准
- 视频为准 + 音频超长：自动用 ffmpeg atempo 变速匹配（不再截断）
- 音频为准 + 音频超长：最后一帧静态延长补足

### AI 文案生成
- System prompt 根据自由度设置动态调整，4 档策略（最小润色 / 较大幅度改写 / 大幅重构 / 彻底重写）
- 默认 50% 自由度对应"较大幅度改写"
- 新增 ⚙️ 文案生成设置弹窗，滑块调节 0-100%
- 防重复点击：批量 AI 修改增加 isRunning() 守卫

### 目录结构简化
- 输出目录从 `outputs/montage/` 简化为 `outputs/`，去掉多余子目录层
- `_get_out_montage_dir` 和 `_get_out_final_dir` 同步更新

### Bug 修复
- 步骤2点击"下一步"卡死：移除 preview_player.stop() 调用（Win Qt6 Media Foundation 死锁）
- 批量 AI 修改重复弹窗：添加防重复点击检查
- 字幕中逗号在 ffmpeg drawtext 中被正确转义
- QMediaPlayer 在页面切换时不再干扰
