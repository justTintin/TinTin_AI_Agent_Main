我的知识库
1.我的知识库页面 -知识背景功能按钮 删除                                 完成                                              完成
2.我的知识库页面-参考素材 下载状态 更新                                 完成
3.素材浏览器 本地下载地址 移除项目 可指定 项目文件内目录只存地址            完成
4.飞书的按钮 隐藏（功能代码保留）                                            完成
5.素材检索页面 关键词搜索和品牌的混合查询检查一下分开接口                    已检查：关键词+品牌是同一接口(/material/search)的混合查询，非分开接口
6.模型配置页面 api地址做成统一配置 各接口在页面单独配置                    完成
   - 页面顶部添加统一服务端地址输入框(compute_server_url)
   - PaddleOCR 使用本地模型，移除了 API 地址输入框
   - 所有Tab的保存按钮(LLM/Ollama/Whisper/CLIP)均保存统一地址+各接口地址
   - 顶部「保存全部」按钮保存全部配置(含VoxCPM参数)
   - 统一地址修改后联动更新 Whisper/CLIP（始终使用统一地址，无需手动维护）
   - VoxCPM 的 API 地址保留手动维护（需手动加 /voxcpm/tts 后缀）
   - 6个模型Tab合并为单个滚动页面，无需切换Tab
   - 模型专属 API 地址输入框置灰只读（禁止手动编辑）：
     · llm_api_url_input（LLM API 地址）
     · llm_vision_api_url_input（视觉模型地址）
     · vox_api_url_input（VoxCPM API 地址）
     · whisper_api_url_input（Whisper ASR 服务地址）
     · clip_api_url_input（CLIP API 地址）
     仅顶部「服务端地址（统一配置）」保持可编辑

7.任务队列页面点击后卡顿崩溃修复                                                  完成
   - 根因：_sync_server_tasks_async 将 _sync_server_tasks 放入 Worker 子线程执行，
     而该方法内部直接操作了 Qt 控件(task_table.insertRow/setItem/setCellWidget 等)，
     在非 GUI 线程操作 Qt 控件导致未定义行为和崩溃
   - 修复：将 _sync_server_tasks 拆分为两个方法：
     · _fetch_server_tasks_data()：仅做 HTTP 请求和数据处理，返回纯数据 dict，不触碰任何 Qt 控件
     · _apply_server_tasks_data(data)：执行所有 GUI 更新，通过 finished 信号回调到主线程执行
   - Worker 对象存为 self._sync_worker 防止 GC 回收导致额外崩溃
   - 「同步服务端」按钮改用 _sync_server_tasks_async，避免点击时阻塞主线程
   - 涉及文件：studio/gui/main_window_pages.py

8.LLM 大语言模型 API 地址根据提供商自动切换                                         完成
   - 提供商下拉框扩展至 7 个选项：DeepSeek / OpenAI / Ollama / 阿里云 DashScope / 智谱 GLM / Moonshot(Kimi) / 自定义
   - 选择预设提供商时 API 地址自动填充并置灰只读（setReadOnly + 深色样式）
   - 选择「自定义」时 API 地址恢复可编辑，允许手动输入任意地址
   - API Key 输入框（含眼睛切换图标）从 UI 隐藏，保留隐藏属性避免保存/测试连接代码崩溃
   - 涉及文件：studio/gui/main_window_pages.py、studio/gui/main_window_aiconfig.py

9.模型配置页各模块按钮区域统一                                                               完成
   - VoxCPM「保存」按钮样式修正：secondary_button → primary_button，宽度 80px → 90px
   - VoxCPM 状态标签从按钮同行移至按钮下方独立行（与 LLM/视觉模型布局一致）
   - Whisper / CLIP 按钮对齐：左对齐 → 右对齐（stretch 前置，与 LLM/视觉模型/VoxCPM 统一）
   - 全部 5 个模块按钮行布局统一：测试连接(secondary_button/110px) + 保存(primary_button/90px)，右对齐
   - 涉及文件：studio/gui/main_window_pages.py

10.智能混剪「按文案智能匹配」模式功能改造                                           完成
   - 「合成视频生成文案」按钮：仅在「随机洗牌」模式显示，按文案模式隐藏（两种模式页面隔离）
   - 新增「🤖 AI 生成文案」按钮：位于文案输入框下方脚本工具栏左侧
     · 根据已勾选的镜头素材描述 + 产品背景 + 时长限制，调用大模型自动生成口播文案
     · 生成的文案按每个镜头一行排列，每行字数受 (时长限制/镜头数) 制约
     · 文案写入 match_script_edit 输入框后用户可编辑，确认后点击「镜头重组」进行智能匹配
   - 「镜头重组」按钮从参数行下移至脚本工具栏右侧（与 AI 生成文案按钮同行）
   - 时长限制控件在两种模式下均可见：
     · 随机洗牌模式：标签"时长限制:"，控制预合成视频总时长
     · 按文案智能匹配模式：标签"文案时长限制:"，控制生成文案的字数/语速上限
   - 新增 GenScriptWorker（BaseWorker 子类）：在后台线程调用 LLM 生成口播文案
   - 新增 _on_gen_script_clicked / _on_gen_script_finished / _on_gen_script_error 回调方法
   - 涉及文件：studio/gui/montage/step2_concat_view.py、studio/gui/video_montage_page.py
